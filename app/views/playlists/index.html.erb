<h1>All Playlists</h1>

<% @playlists.each do |playlist| %>
  <section id= "playlist-<%= playlist.id %>">
    <%= playlist.name %>
    <% playlist.songs.each do |song| %>
      <%= song.title %>
    <% end %>
  </section>
<% end %>
